apiVersion: v1
kind: Service

metadata:
  name: smart-city-service

spec:
  type: NodePort

  selector:
    app: smart-city

  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30007